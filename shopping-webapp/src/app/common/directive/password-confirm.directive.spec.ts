import { PasswordConfirmDirective } from './password-confirm.directive';

describe('PasswordConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
