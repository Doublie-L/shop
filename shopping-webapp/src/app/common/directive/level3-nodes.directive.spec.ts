import { Level3NodesDirective } from './level3-nodes.directive';

describe('Level3NodesDirective', () => {
  it('should create an instance', () => {
    const directive = new Level3NodesDirective();
    expect(directive).toBeTruthy();
  });
});
