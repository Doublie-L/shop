import { ValueToNameObjPipe } from './value-to-name-obj.pipe';

describe('ValueToNameObjPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueToNameObjPipe();
    expect(pipe).toBeTruthy();
  });
});
